# Vibe Audit Morning Scan
**Wednesday, July 8, 2026** | Hybrid scan (local + cross-repo code search)

> **Note:** The full 159-repo batch scan could not run — this environment's
> egress policy restricts GitHub API access to `jackdog668/vibeaudit` only.
> Results below combine a local scan of vibeaudit with cross-repo code
> searches via the GitHub MCP server (163 repos searched). The GitHub Actions
> workflow remains the best path for a full batch scan.

---

## vibeaudit Self-Scan — Grade B (was A yesterday)

| Severity | Count | Details |
|----------|-------|---------|
| Critical | 1 | `flatted@<=3.4.1` — Prototype Pollution via `parse()` |
| Warning | 2 | `brace-expansion@<1.1.13` ReDoS; `js-yaml@4.0.0-4.1.1` quadratic DoS |

**Action required:** Run `npm audit fix` or update `flatted` to patch the prototype pollution CVE.

---

## Cross-Repo Code Search (163 repos)

Searched all `jackdog668/*` repositories via GitHub code search for the most critical vibe-coding anti-patterns.

### Clean (no findings)

| Check | Result |
|-------|--------|
| Hardcoded API keys (`sk_live`, `sk_test`, `AIza`, `ghp_`, `glpat-`) | None found |
| Committed `.env` files | None — only `.env.example` files |
| Open Firebase/Firestore rules (`allow read, write: if true`) | None in production code |
| `eval()` / `new Function()` with dynamic input | None in user code |
| `exec()` / `child_process` command injection | None found |
| Stripe webhook — missing signature verification | `saas-starter` verifies correctly |
| Supabase service-role key in client code | None found |
| JWT/auth token mishandling | None found |

### Issues Found

| Repo | Issue | Severity | Details |
|------|-------|----------|---------|
| `Chromatic-Illusion-Weaponizerr` | CORS wildcard | Warning | `server.js` sets `Access-Control-Allow-Origin: *` — any website can call the API |
| `sref-scanner` | Open CORS | Warning | `backend/server.js` calls `cors()` with no origin restriction |
| `blazing-schrodinger` | Open CORS | Warning | `server/server.js` calls `cors()` with no origin restriction |

### Notable Positives

- **`saas-starter`**: Stripe webhook handler at `app/api/stripe/webhook/route.ts` properly verifies signatures with `STRIPE_WEBHOOK_SECRET`.
- **`digitalalchemy-dev`**: Supabase server client uses `"server-only"` import guard — service-role key cannot leak to client.
- **`exo-spirit`**: Proper client/server Supabase split (`@supabase/ssr` on both sides).
- **`screenshot-pools`**: CORS is origin-allowlisted, not wildcarded.

---

## Comparison with Yesterday (July 7)

| Metric | July 7 | July 8 |
|--------|--------|--------|
| Repos fully scanned | 8 | 1 (local) |
| Cross-repo code searches | — | 163 repos searched |
| vibeaudit grade | A | B (new: vulnerable `flatted` dep) |
| Siftly grade | F (34 criticals) | Not re-scanned (egress blocked) |
| New critical findings | 0 | 1 (`flatted` prototype pollution in vibeaudit) |

**Siftly** remains the highest-risk repo (34 criticals yesterday — all missing-auth on Next.js API routes). No code changes pushed since yesterday's scan.

---

## Recommendations

1. **Fix `flatted` vulnerability in vibeaudit** — `npm audit fix` or pin `flatted@>=3.4.2`
2. **Lock down CORS** in `Chromatic-Illusion-Weaponizerr`, `sref-scanner`, `blazing-schrodinger` — replace `cors()` / `*` with explicit origin allowlists
3. **Add auth to Siftly API routes** — 34 unprotected handlers (GET/POST/PUT/DELETE) need authentication checks
4. **Restore full batch scan** — re-enable the GitHub Actions morning-scan workflow with a `GITHUB_TOKEN` that has `repo` scope, or widen the egress policy for this Claude Code environment

---
*Generated by Vibe Audit v1.1.0 + Claude Code cross-repo search — 2026-07-08*
